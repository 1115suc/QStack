# 🐧 Linux 学习笔记

## 📁 文件和目录操作

### 基础命令
- `ls`：列出目录内容
  ```bash
  ls                    # 显示当前目录文件
  ls -l                 # 详细信息显示
  ls -la                # 显示所有文件（包括隐藏文件）
  ```

  🔍 拓展：`ll` 是 `ls -l` 的别名，在大多数系统中可用

- `cd`：切换目录
  ```bash
  cd /home              # 切换到 /home 目录
  cd ..                 # 返回上级目录
  cd ~                  # 切换到用户主目录
  cd -                  # 切换到上次所在目录
  ```


- `pwd`：显示当前工作目录
  ```bash
  pwd                   # 显示完整路径
  ```


- `mkdir`：创建目录
  ```bash
  mkdir newdir          # 创建新目录
  mkdir -p a/b/c        # 递归创建多级目录
  ```


- `rm`：删除文件或目录
  ```bash
  rm file.txt           # 删除文件
  rm -r directory       # 递归删除目录
  rm -rf directory      # 强制递归删除（谨慎使用）
  ```


- `cp`：复制文件或目录
  ```bash
  cp file1.txt file2.txt     # 复制文件
  cp -r dir1 dir2            # 复制目录
  ```


- `mv`：移动或重命名文件
  ```bash
  mv oldname.txt newname.txt # 重命名文件
  mv file.txt /home/         # 移动文件
  ```


---

## 🔍 文件查看和编辑

### 文件查看命令
- `cat`：显示文件内容
  ```bash
  cat file.txt          # 显示全部内容
  cat file1.txt file2.txt > combined.txt  # 合并文件
  ```


- `less` / `more`：分页查看文件
  ```bash
  less file.txt         # 分页查看，可上下滚动
  more file.txt         # 分页查看，只能向下翻页
  ```


- `head` / `tail`：查看文件头部/尾部
  ```bash
  head -n 10 file.txt   # 查看前10行
  tail -n 10 file.txt   # 查看后10行
  tail -f logfile.log   # 实时监控文件末尾变化
  ```


### 文本编辑器

#### vi/vim 编辑器
- `vi filename`：打开文件编辑
- 基本模式：
    - **命令模式**：默认模式，可执行各种命令
    - **插入模式**：按 `i`、`a`、`o` 进入，可编辑文本
    - **底线命令模式**：按 `:` 进入，可执行保存退出等命令

- 常用操作：
  ```bash
  # 命令模式下
  i                     # 在光标前插入
  a                     # 在光标后插入
  o                     # 在下一行插入新行
  dd                    # 删除整行
  yy                    # 复制整行
  p                     # 粘贴
  :w                    # 保存
  :q                    # 退出
  :wq                   # 保存并退出
  :q!                   # 强制退出不保存
  /keyword              # 搜索关键字
  ```


🔍 拓展：`nano` 是另一个简单易用的文本编辑器，适合初学者

---

## 🔧 系统信息和进程管理

### 系统信息查看
- `ps`：查看进程状态
  ```bash
  ps aux                # 显示所有进程详细信息
  ps -ef                # 显示完整格式的进程信息
  ```


- `top` / `htop`：实时查看系统进程
  ```bash
  top                   # 显示系统资源使用情况
  htop                  # 更友好的交互式进程查看器
  ```


- `df`：查看磁盘空间使用情况
  ```bash
  df -h                 # 以人类可读格式显示
  ```


- `du`：查看目录空间占用
  ```bash
  du -sh directory      # 显示目录总大小
  du -h --max-depth=1   # 显示一级子目录大小
  ```


- `free`：查看内存使用情况
  ```bash
  free -h               # 以人类可读格式显示内存信息
  ```


### 进程管理
- `kill`：终止进程
  ```bash
  kill PID              # 终止指定PID的进程
  kill -9 PID           # 强制终止进程
  ```


- `nohup`：后台运行命令
  ```bash
  nohup command &       # 后台运行命令，关闭终端也不停止
  ```


🔍 拓展：`jobs` 查看后台任务，`fg` 将后台任务调至前台

---

## 🔍 搜索和查找

### 文件搜索
- `find`：查找文件
  ```bash
  find /path -name "*.txt"     # 按名称查找
  find /path -type d           # 查找目录
  find /path -mtime -7         # 查找7天内修改的文件
  ```


- `which`：查找命令位置
  ```bash
  which java            # 查找 java 命令的位置
  ```


- `whereis`：查找二进制文件、源码、手册位置
  ```bash
  whereis python        # 查找 python 相关文件
  ```


### 内容搜索
- `grep`：在文件中搜索文本
  ```bash
  grep "pattern" file.txt      # 搜索匹配行
  grep -r "pattern" /path      # 递归搜索目录
  grep -i "pattern" file.txt   # 忽略大小写搜索
  ```


🔍 拓展：`locate` 快速查找文件（需定期更新数据库）

---

## 🌐 网络和权限管理

### 网络相关
- `ping`：测试网络连通性
  ```bash
  ping google.com       # 测试与目标主机的连接
  ```


- `wget` / `curl`：下载文件
  ```bash
  wget url              # 下载文件
  curl url              # 获取网页内容
  ```


- `ssh`：远程登录
  ```bash
  ssh user@host         # 连接到远程主机
  ```


### 权限管理
- `chmod`：修改文件权限
  ```bash
  chmod 755 file.sh     # 设置权限为 rwxr-xr-x
  chmod +x file.sh      # 添加执行权限
  ```


- `chown`：修改文件所有者
  ```bash
  chown user:group file # 修改文件所有者和组
  ```


- `sudo`：以超级用户权限执行命令
  ```bash
  sudo command          # 以管理员权限执行命令
  ```


---

## ⚙️ 常用工具和技巧

### 压缩和解压
- `tar`：打包和解包文件
  ```bash
  tar -czf archive.tar.gz directory    # 创建压缩包
  tar -zxvf archive.tar.gz             # 解压文件 -z: gzip压缩 -x: 解压 -v: 显示进度 -f: 覆盖同名文件
  ```


- `zip` / `unzip`：ZIP格式压缩
  ```bash
  zip archive.zip file.txt            # 创建ZIP文件
  unzip archive.zip                   # 解压ZIP文件
  ```


### 管道和重定向
- `|`：管道符，将前一个命令输出作为后一个命令输入
  ```bash
  ps aux | grep java    # 查找java进程
  ```


- `>` / `>>`：重定向输出
  ```bash
  echo "text" > file.txt     # 覆盖写入文件
  echo "text" >> file.txt    # 追加写入文件
  ```


🔍 拓展：`tee` 命令可以同时输出到屏幕和文件

### 环境变量
- `export`：设置环境变量
  ```bash
  export PATH=$PATH:/new/path        # 添加新路径到PATH
  export MY_VAR="value"              # 设置环境变量
  ```


- `env`：查看所有环境变量
  ```bash
  env                   # 显示所有环境变量
  ```

### 日志分析
```bash
tail -f /var/log/application.log | grep ERROR    # 实时监控错误日志
awk '{print $1,$4}' access.log                   # 使用 awk 处理日志
```

### 端口和服务管理
```bash
netstat -tuln         # 查看监听端口
ss -tuln              # 更现代的端口查看工具
systemctl start service_name    # 启动服务
systemctl status service_name   # 查看服务状态
```


### 性能监控
```bash
iostat                # 查看IO统计信息
vmstat                # 查看虚拟内存统计
sar                   # 系统活动报告工具
```