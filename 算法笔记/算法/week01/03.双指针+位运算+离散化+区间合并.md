# 双指针算法（Two Pointers）🎯

## 1. 核心思想 💡
- 使用**两个指针**（索引）在数组/链表上协同遍历
- 通过指针的**移动规则**和**位置关系**高效解决问题
- 常见模式：同向移动、相向移动、快慢指针

---

## 2. 算法类型 🔢

### 2.1 同向双指针（快慢指针）🚶‍♂️🚶‍♂️
- **应用场景**：原地修改数组、链表去重、判断循环
- **移动规则**：
    - 慢指针 `slow` 指向有效位置
    - 快指针 `fast` 向前探索

#### 示例代码：移除有序数组重复元素
```java
public int removeDuplicates(int[] nums) {
    if (nums.length == 0) return 0;
    int slow = 0;
    for (int fast = 1; fast < nums.length; fast++) {
        if (nums[fast] != nums[slow]) {
            slow++;
            nums[slow] = nums[fast];
        }
    }
    return slow + 1;
}
```


---

### 2.2 相向双指针 ↔️
- **应用场景**：有序数组求和、回文判断、盛水问题
- **移动规则**：
    - 左指针 `left` 从起点向右
    - 右指针 `right` 从终点向左

#### 示例代码：两数之和 II
```java
public int[] twoSum(int[] nums, int target) {
    int left = 0, right = nums.length - 1;
    while (left < right) {
        int sum = nums[left] + nums[right];
        if (sum == target) {
            return new int[]{left + 1, right + 1};
        } else if (sum < target) {
            left++;
        } else {
            right--;
        }
    }
    return new int[]{-1, -1};
}
```
---
## 3. 时间复杂度分析 ⏱️
| 算法类型       | 时间复杂度 | 空间复杂度 |
|----------------|------------|------------|
| 同向双指针     | O(n)       | O(1)       |
| 相向双指针     | O(n)       | O(1)       |
---
## 4. 使用场景 🎯
1. **数组处理**：
    - 有序数组求和/三数之和
    - 移除指定元素
2. **链表操作**：
    - 判断环形链表
    - 寻找链表中间节点
3. **字符串处理**：
    - 回文字符串验证
    - 最小覆盖子串
4. **区间问题**：
    - 合并重叠区间
    - 区间列表的交集
---
## 5. 解题技巧 🧠
1. **指针移动条件**：明确指针移动的触发条件（值比较、状态变化等）
2. **边界处理**：特别注意指针越界和空输入情况
3. **循环不变式**：保持循环中指针关系的逻辑一致性

> 通过合理选择指针类型和移动策略，可将暴力解法O(n²)优化至O(n)


---

## 6. 实例题目 📌

#### 题目描述：
给定一个长度为 n 的字符串，请找出最长的不包含重复的数的连续区间，输出它的长度。
#### 输入格式：
- 共一行，输入一串字符串 s。
#### 输出格式：
- 共一行，包含一个整数，表示最长不包含重复字符的连续子序列的长度。

#### 数据范围：
- 1 ≤ n ≤ 10^5

#### 解题思路：
- 使用滑动窗口思想，维护一个不含重复字符的窗口
- `i` 为右指针，不断向右扩展窗口
- `j` 为左指针，当出现重复字符时，向右收缩窗口
- 使用 `flag` 数组记录窗口内字符出现次数
- 时间复杂度：O(n)，每个字符最多被访问两次
- 空间复杂度：O(1)，固定大小为256的数组

#### 输入样例：
```
abcdabc
```

#### 输出样例：
```
4
```

```java
import java.util.Scanner;

public class TwoPointers {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        String str = in.nextLine();
        char[] flag = new char[256];

        int ans = 0;
        for (int i = 0 , j = 0 ; i < str.length(); i++) {
            flag[str.charAt(i)]++;
            while (flag[str.charAt(i)] > 1) {
                flag[str.charAt(j)]--;
                j++;
            }
            ans = Math.max(ans, i - j + 1);
        }
        System.out.println(ans);
    }
}
```


# 位运算（Bitwise Operations）🧮

## 1. 核心思想 💡
- 对数据的**二进制表示**进行直接操作
- 利用**位级别的高效计算**替代常规运算

---

## 2. 运算符类型 🔢

| 运算符 | 描述 |
|--------|------|
| `&`    | 按位与，全1为1 |
| `\|`   | 按位或，有1为1 |
| `^`    | 按位异或，相同为0，不同为1 |
| `~`    | 按位取反，0变1，1变0 |
| `<<`   | 左移，高位丢弃，低位补0 |
| `>>`   | 右移（带符号），低位丢弃，高位补符号位 |
| `>>>`  | 右移（无符号），低位丢弃，高位补0 |

---

### 2.1 常见应用模式 🧪

#### 获取一个整数的二进制中第 `k` 位的值
```java
int getBit(int n, int k) {
    return (n >> k) & 1;
}
```


#### 将一个整数的二进制中第 `k` 位设为 `1`
```java
int setBit(int n, int k) {
    return n | (1 << k);
}
```


#### 将一个整数的二进制中第 `k` 位设为 `0`
```java
int clearBit(int n, int k) {
    return n & ~(1 << k);
}
```


#### 使用异或交换两个整数
```java
void swap(int a, int b) {
    a = a ^ b;
    b = a ^ b;
    a = a ^ b;
}
```
---
## 3. 时间复杂度分析 ⏱️
| 运算类型       | 时间复杂度 | 空间复杂度 |
|----------------|------------|------------|
| 位运算         | O(1)       | O(1)       |
---
## 4. 使用场景 🎯
1. **状态压缩**：
    - 将多个布尔状态压缩到一个整数中
    - 如：迷宫问题中的方向标记、子集表示
2. **快速判断**：
    - 判断奇偶性：`n & 1`
    - 判断是否是2的幂：`(n & (n - 1)) == 0`
3. **加密解密**：
    - 异或加密：简单混淆数据
4. **集合操作**：
    - 用位掩码模拟集合交并补操作
5. **性能优化**：
    - 替代乘除法：左移相当于乘2，右移相当于除2
6. **位图（Bitmap）管理**：
    - 高效存储和查询大量布尔标志
---
## 5. 解题技巧 🧠
1. **理解二进制逻辑**：熟悉二进制下各种操作的效果
2. **掩码（Mask）运用**：灵活使用掩码提取/修改特定比特位
3. **位移结合逻辑运算**：如 `(x >> 3) & 0xFF` 提取高位字节
4. **注意符号扩展**：有符号右移 `>>` 和无符号右移 `>>>` 的区别
5. **边界处理**：特别注意 `0`、负数和溢出情况
6. **位计数技巧**：利用 `n & (n - 1)` 快速消除最右边的 `1`

> 通过合理使用位运算，可以极大提升程序效率，特别是在嵌入式系统、算法竞赛等领域。

# 离散化算法（Discretization）📚

## 1. 核心思想 💡
- **将大范围数值映射到小范围索引**，便于使用数组等结构高效处理数据
- 主要用于处理**稀疏但有序的数据**
- 常见于区间统计、坐标压缩、频率统计等问题中

---

## 2. 算法步骤 🔢

### 2.1 基本流程
1. **收集所有待处理的原始数据值**
2. **去重排序**，得到唯一有序序列
3. **为每个值分配一个连续的整数索引（rank）**
4. **用该索引代替原值进行后续操作**

### 2.2 典型应用
- 配合前缀和、差分数组使用
- 处理超出内存限制的坐标/索引问题
- 统计高频元素时优化空间使用

---

## 3. 示例代码 🧪

#### 示例：对一组整数进行离散化处理
```java
import java.util.*;

public class Discretization {
    public static void main(String[] args) {
        int[] data = {100, 2, 50, 100, 2, 200};
        List<Integer> list = new ArrayList<>();

        // 收集所有原始值
        for (int num : data) {
            list.add(num);
        }

        // 去重并排序
        Set<Integer> uniqueSet = new HashSet<>(list);
        List<Integer> sortedList = new ArrayList<>(uniqueSet);
        Collections.sort(sortedList);

        // 构建映射表：value -> index
        Map<Integer, Integer> map = new HashMap<>();
        for (int i = 0; i < sortedList.size(); i++) {
            map.put(sortedList.get(i), i + 1); // 从1开始编号，方便前缀和使用
        }

        // 输出映射结果
        for (int num : data) {
            System.out.print(map.get(num) + " ");
        }
    }
}
```


---

## 4. 时间复杂度分析 ⏱️
| 操作           | 时间复杂度         |
|----------------|--------------------|
| 排序            | O(n log n)         |
| 构建哈希映射     | O(n)               |
| 查找映射值       | O(1)               |

> 总体时间复杂度：**O(n log n)**  
> 空间复杂度：**O(n)**

---

## 5. 使用场景 🎯
1. **坐标压缩**：
    - 如地图坐标点过大，无法直接作为数组下标
2. **频率统计**：
    - 对海量稀疏数据统计出现次数
3. **配合其他算法**：
    - 与前缀和、线段树、树状数组结合使用
4. **数据预处理**：
    - 将字符串或其他类型转换为数字索引

---

## 6. 解题技巧 🧠
1. **保留原始顺序信息**：排序后不能打乱原始数据的位置关系
2. **注意重复值处理**：去重是关键步骤，避免浪费索引资源
3. **合理选择索引起始位置**：常从 `1` 开始以方便前缀和使用
4. **灵活使用容器**：
    - Java 中推荐使用 `TreeSet` 或 `ArrayList + sort`
    - 映射使用 `HashMap` 快速查找
5. **边界值检查**：确保映射正确性，特别是最大最小值

> 离散化是一种典型的“空间换时间”策略，能有效降低数据规模，提高算法效率。

# 区间合并算法（Interval Merging）📚

## 1. 核心思想 💡
- **将有重叠或相邻的区间进行合并**，最终得到一组互不重叠且有序的区间集合
- 常用于处理**时间/空间区间的重叠问题**
- 典型策略是**排序 + 贪心遍历**

---

## 2. 算法步骤 🔢

### 2.1 基本流程
1. **对区间按起始点排序**（默认升序）
2. **逐个遍历区间**：
    - 如果当前结果列表为空，或当前区间与结果中最后一个区间**无重叠**，则直接加入
    - 否则，**合并到最后一个区间**
3. 返回合并后的区间列表

### 2.2 变体情况
- 合并后仍需保持区间有序
- 支持动态插入新区间时可考虑使用 `TreeSet` 或线段树优化

---

## 3. 示例代码 🧪

#### 示例：合并所有有重叠的区间
```java
import java.util.*;

class Interval {
    int start, end;
    Interval(int s, int e) {
        start = s;
        end = e;
    }
}

public class MergeIntervals {
    public static List<Interval> merge(List<Interval> intervals) {
        if (intervals.size() == 0) return new ArrayList<>();

        // 按照起点排序
        intervals.sort((a, b) -> a.start - b.start);

        List<Interval> merged = new ArrayList<>();
        merged.add(intervals.get(0));

        for (Interval curr : intervals) {
            Interval last = merged.get(merged.size() - 1);
            if (curr.start <= last.end) {
                // 合并区间
                last.end = Math.max(last.end, curr.end);
            } else {
                merged.add(curr);
            }
        }

        return merged;
    }

    public static void main(String[] args) {
        List<Interval> intervals = new ArrayList<>();
        intervals.add(new Interval(1, 3));
        intervals.add(new Interval(2, 6));
        intervals.add(new Interval(8, 10));
        intervals.add(new Interval(15, 18));

        List<Interval> result = merge(intervals);
        for (Interval interval : result) {
            System.out.println("[" + interval.start + ", " + interval.end + "]");
        }
    }
}
```


---

## 4. 时间复杂度分析 ⏱️
| 操作       | 时间复杂度     |
|------------|----------------|
| 排序       | O(n log n)     |
| 遍历合并   | O(n)           |

> 总体时间复杂度：**O(n log n)**  
> 空间复杂度：**O(n)**（结果存储）

---

## 5. 使用场景 🎯
1. **日程安排冲突检测**
    - 如会议时间重叠判断
2. **资源调度管理**
    - CPU任务调度、内存分配等
3. **数据范围压缩**
    - 日志系统中的时间窗口合并
4. **地理信息处理**
    - 地图上区域重叠的可视化展示
5. **配合其他算法**
    - 与扫描线算法结合使用

---

## 6. 解题技巧 🧠
1. **排序优先级**：一般先按左端点排序，必要时再考虑右端点
2. **边界条件处理**：
    - 注意空输入和单个区间的情况
    - 判断是否需要深拷贝
3. **合并逻辑清晰**：始终比较当前区间与上一个已合并区间的终点
4. **返回类型选择**：根据需求决定是否返回原类型或简化为二维数组
5. **可扩展性设计**：若需频繁添加新区间，可维护有序结构

> 区间合并是一种典型的贪心算法应用，通过排序+一次遍历即可高效解决重叠区间问题。