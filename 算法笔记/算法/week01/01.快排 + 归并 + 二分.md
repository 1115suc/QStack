# 快速排序（分治）🚀

## 1. 选择基准值🎯
- 从数组中选择一个元素作为基准值。通常选择第一个元素、最后一个元素或中间元素。

## 2. 调整区间🔄
- 将数组分为两部分：一部分小于基准值，另一部分大于基准值。
- 使用两个指针（`i` 和 `j`）从数组的两端向中间移动，交换不符合条件的元素，直到指针相遇。

## 3. 递归排序🔁
- 对基准值左边的子数组递归调用快速排序。
- 对基准值右边的子数组递归调用快速排序。

## 4. 合并✨
- 由于快速排序是原地排序，不需要额外的合并步骤。递归调用结束后，数组已经有序。

## 5. 快速排序代码实现(Java)💻
```java
import java.util.Scanner;

class QuickSort {
    public static void quickSort(int q[], int l, int r) {
        if (l >= r) return;
        int x = q[l], i = l - 1, j = r + 1;
        while (i < j) {
            do i++; while (q[i] < x);
            do j--; while (q[j] > x);
            if (i < j) {
                int temp = q[i];
                q[i] = q[j];
                q[j] = temp;
            }
        }

        // int x = q[l] 取左看右指针
        // int x = q[r] 取右看左指针
        quickSort(q, l, j);
        quickSort(q, j + 1, r);
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int q[] = {5, 3, 2, 4, 1, 7, 6, 9, 8, 0};

        quickSort(q, 0, q.length-1);
        for(int num: q){
            System.out.print(num + " ");
        }
    }
}
```

--- 
# 归并排序（分治）🌐

## 1. 分解数组✂️
- 将待排序数组递归地分成两个近似相等的子数组。
- 持续分解直到每个子数组只包含一个元素（天然有序）。

## 2. 递归排序🔁
- 对左半部分子数组递归调用归并排序。
- 对右半部分子数组递归调用归并排序。

## 3. 合并有序数组🤝
- 创建临时数组存放合并结果。
- 使用双指针法合并两个有序子数组：
    - 指针 `i` 遍历左子数组。
    - 指针 `j` 遍历右子数组。
    - 比较两指针指向的元素，选择较小的放入临时数组。
    - 将剩余元素拷贝到临时数组末尾。
- 将临时数组拷贝回原数组对应位置。

## 4. 完成排序🎉
- 当所有递归调用返回时，原始数组已完全有序。

## 5. 归并排序代码实现(Java)💻
```java
import java.util.Scanner;

public class MergeSort {

    static final int N = 100010;
    static int temp[] = new int[N];

    public static void mergeSort(int q[], int l, int r) {
        if (l >= r) return;

        int mid = l + r >> 1;

        mergeSort(q, l, mid);
        mergeSort(q, mid + 1, r);

        int k = 0, i = l, j = mid + 1;
        while(i <= mid && j <= r)
            if (q[i] <= q[j]) temp[k++] = q[i++];
            else temp[k++] = q[j++];
        while (i <= mid) temp[k++] = q[i++];
        while (j <= r) temp[k++] = q[j++];

        for (i = l, j = 0; i <= r; i++, j++) q[i] = temp[j];
    }
    
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int q[] = {5, 3, 2, 4, 1, 7, 6, 9, 8, 0};
        mergeSort(q, 0, q.length-1);
        for(int num: q){
            System.out.print(num + " ");
        }
    }
}
```
--- 

# 整数二分
## 1. 确定搜索范围 🎯
+ 初始化两个指针：left 和 right，分别指向数组的起始和结束位置。

## 2. 计算中间值 🧮
+ 计算中间位置 `mid = ( left + right + 1 ) / 2`，避免整数溢出。

## 3. 比较中间值 🔄
+ 如果 arr[mid] 等于目标值 target，则返回 mid，查找成功。
+ 如果 arr[mid] 小于 target，则更新 left = mid + 1，继续在右半部分查找。
+ 如果 arr[mid] 大于 target，则更新 right = mid - 1，继续在左半部分查找。

## 4. 循环查找 🔁
+ 重复步骤2和步骤3，直到 left 超过 right，表示查找失败。

## 5. 返回结果 📌
+ 如果找到目标值，返回其索引；否则返回 -1 表示未找到。

## 6. 二分查找代码实现(Java)
```java
import java.util.Scanner;

public class BinarySearch {
  public static void main(String[] args) {
    Scanner sc = new Scanner(System.in);
    int q[] = {3, 4, 5, 7, 7, 8, 9, 10, 11, 12};
    int l = 0 , r = q.length-1;
    int target = sc.nextInt();

    // 从左边开始二分查找第一个符合条件的数
    while (l < r) {
      int mid = l + r >> 1;
      if(q[mid] >= target) r = mid;
      else l = mid + 1;
    }

    if (q[l] != target) System.out.println("-1 -1");

      // 从右边开始二分查找第一个符合条件的数
    else {
      System.out.print(l + " ");
      l = 0;
      r = q.length-1;
      while (l < r) {
        int mid = l + r + 1 >> 1;
        if(q[mid] <= target) l = mid;
        else r = mid - 1;
      }
      System.out.print(l);
    }
  }
}
```