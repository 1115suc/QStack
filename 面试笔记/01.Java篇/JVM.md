# JVM 面试

## 1. Java 的执行流程

- **编写源代码**：创建 .java 文件
- **编译成字节码**：javac 编译为 .class 文件（JVM 可识别的字节码）
- **类加载**：类加载器加载 .class 文件，经过==加载 → 链接（验证、准备、解析）→ 初始化==
- **执行字节码**：解释器逐条翻译执行，JIT 监控热点代码
- **JIT 编译优化**：热点代码编译为机器码缓存，提升执行效率
- **垃圾回收**：GC 线程自动回收无用对象，释放堆内存
- **程序退出**：main 方法结束，JVM 清理后退出
